extends layout

block content
    link(rel='stylesheet', href="/css/nav2.css")
    link(rel='stylesheet', href="/css/TopCanciones.css")
    body
        include nav2
        .container
            header
                .atras
                    p
                        a(href="/Perfil") Atras
                h2= pagina
            .contenido
                p Añadir Cancion Favorita
                    a(href="#")#agregar Añadir
                form(action="" method="post")#nuevaCancion.oculto
                    input(type="text" name="cancion")
                    input(type="submit" value="Añadir" name="Añadir")
                ul
                    if datos.canciones
                        for item in datos.canciones
                            li
                                a(href="#")= item
        script.
            document.getElementById("agregar").addEventListener("click", function () {
                document.getElementById("nuevaCancion").classList.toggle("oculto")
            })